#jinja2: trim_blocks: True,lstrip_blocks: True
{#############################################

Purpose:
Configure AAA commands for dellos10 Devices

###############################################}

ip domain-name {{ domain.name }}
{% if domain.vrf %}
ip name-server vrf {{ domain.vrf }} {{ domain.server|join(' ') }}
{% else %}
ip name-server {{ domain.server|join(' ') }}
{% endif %}
